section.centralize-area(ng-show="onLoading == true")
   img(src="/assets/images/loading.gif" style="width: 40px; heigth: 40px")

section.centralize-area(ng-show="typeformData")
  br
  br
  h1 Estatísticas
  br
  br
  table.table.table-striped.table-hover
    thead
      tr
        th(ng-repeat="(k,v) in typeformData.stats.responses") {{ k }}
    tbody
      tr
        td(ng-repeat="(k,v) in typeformData.stats.responses") {{ v }}
