section.centralize-area(ng-controller="TypeformCtrl")
  br
  br
  h1 Estatísticas para {{ currentForm }}
  br
  br
  p {{ stats }}
  img(ng-show="onLoading" src="/assets/images/loading.gif")
  table.table.table-striped.table-hover(ng-show="onLoading === false && stats")
    thead
      tr
        th(ng-repeat="(k,v) in stats.responses") {{ k }}
    tbody
      tr
        td(ng-repeat="(k,v) in stats.responses") {{ v }}

    a.btn.btn-default(href="/#!/formularios/{{ currentForm }}/questions") Ver respostas
