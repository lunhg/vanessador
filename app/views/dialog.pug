div.ng-modal(ng-show='dialogShown')
  div.ng-modal-overlay(ng-click='hideModal()')
  div.ng-modal-dialog(ng-style='dialogStyle')
    div.ng-modal-close(ng-click='hideModal()')
    div.ng-modal-dialog-content {{ dialogMessage }}

