section.col-md-12.centralize-area(ng-show="user")
  br
  br
  h1 Registre um formulário
  br
  br
  form 
    fieldset 
    - var form = {'input_typeform_nome':{'type':'text', 'placeholder':'nome'}, 'input_typeform_tags':{'type':'text', 'placeholder':'#tags... (ex. #alunos #workshop)'}, 'input_typeform_uuid':{'type':'text', 'placeholder':'lD2u6E'}}
    - for(var key in form)
      - var val = form[key]
      div.form-group
        label.col-lg-2.control-label(for=""+key)
          div.col-lg-10 
            input.form-control(style="width:300px;" type=""+val.type id=""+key placeholder=""+val.placeholder)

    a.btn.btn-default(ng-click="onNovo('formularios', 'input_typeform', 'nome', 'tags', 'uuid')") Registrar
